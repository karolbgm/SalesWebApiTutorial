{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "ConnectionStrings": {
    "AppDbContex": "server=localhost\\sqlexpress;database=TestApiDb;trusted_connection=true;trustServerCertificate=true;"
  }
}

//we add the connection string here - in this case, we're creating a new database

//this is the file being used when we're building/debugging

//appsetting.json - that's used when we're in production
